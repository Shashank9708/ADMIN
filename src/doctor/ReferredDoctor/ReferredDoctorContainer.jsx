import React from 'react';
import {ReferredDoctor} from './ReferredDoctor';

export const ReferredDoctorContainer = (props) => {

    return (
      <div className="page-container">
        <ReferredDoctor/>
      </div>
    );

}
