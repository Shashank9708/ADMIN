import React from 'react';
import {Completed} from './Completed';

export const CompletedContainer = (props) => {

    return (
      <React.Fragment>
        <Completed/>
      </React.Fragment>
    );

}
