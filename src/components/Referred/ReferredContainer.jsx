import React from 'react';
import {Referred} from './Referred';

export const ReferredContainer = (props) => {

    return (
      <div className="page-container">
        <Referred/>
      </div>
    );

}
