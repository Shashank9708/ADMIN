import React from 'react';
import {VideoAppointments} from './VideoAppointments';

export const VideoAppointmentsContainer = (props) => {

    return (
      <div className="page-container">
        <VideoAppointments/>
      </div>
    );

}
