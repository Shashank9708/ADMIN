import React from 'react';
import {SendNotification} from './SendNotification';

export const SendNotificationContainer = (props) => {

    return (
      <div className="page-container">
        <SendNotification/>
      </div>
    );

}
