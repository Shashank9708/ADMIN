import React from 'react';
import {Appointments} from './Appointments';

export const AppointmentsContainer = (props) => {

    return (
      <div className="page-container">
        <Appointments/>
      </div>
    );

}
